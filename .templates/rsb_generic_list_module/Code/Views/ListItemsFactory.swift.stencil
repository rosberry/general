{% include "header.stencil" %}

import GenericModule
import CollectionViewTools

final class {{ name | upperFirstLetter }}Factory: GenericSectionItemsFactory<{{ name | upperFirstLetter }}State, GeneralCollectionViewDiffSectionItem, {{ name | upperFirstLetter }}ModuleDependencies, {{ name | upperFirstLetter }}ViewOutput> {

    override func makeSectionItems(state: {{ name | upperFirstLetter }}State) -> [GeneralCollectionViewDiffSectionItem] {
        []
    }
}
