{% include "header.stencil" %}

import CollectionViewTools

final class {{ name | upperFirstLetter }}ViewModel {

    let listSectionItems: [GeneralCollectionViewDiffSectionItem]

    init(state: {{ name | upperFirstLetter }}State, listItemsFactory: {{ name | upperFirstLetter }}ListItemsFactory) {
        listSectionItems = listItemsFactory.makeSectionItems(state: state)
    }
}
