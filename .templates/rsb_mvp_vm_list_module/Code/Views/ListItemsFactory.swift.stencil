{% include "header.stencil" %}

import CollectionViewTools

final class {{ name }}ListItemsFactory {

    weak var viewController: {{ name }}ViewController?
    weak var output: {{ name }}ViewOutput?

    func makeSectionItems(state: {{ name }}State) -> [GeneralCollectionViewDiffSectionItem] {
        return []
    }
}
