{% include "header.stencil" %}

import CollectionViewTools

final class {{ name | upperFirstLetter }}ListItemsFactory {

    weak var viewController: {{ name | upperFirstLetter }}ViewController?
    weak var output: {{ name | upperFirstLetter }}ViewOutput?

    func makeSectionItems(state: {{ name | upperFirstLetter }}State) -> [GeneralCollectionViewDiffSectionItem] {
        return []
    }
}
